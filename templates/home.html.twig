{% extends "base.html.twig" %}

{% block title %}
Accueil - HAY
{% endblock %}

{% block body %}

{{ include("menu.html.twig") }}
{{ include("statusForm.html.twig") }}
{#{{ include("menu.html.twig") }}#}

{% endblock %}

{% block js %}
<script>

function changeColor(e) {
    color = "c" + e['target'].id
    textarea = document.getElementsByClassName("textarea")
    classes = textarea[0].className
    classes = classes.split("c")
    classes[0] += color
    textarea[0].className = classes[0]
}

document.getElementsByName('color').forEach(function(element) {
    element.addEventListener('change', changeColor)
})

</script>
{% endblock %}